# 数学英雄 · 开发日志

> 将初中生数学成绩转化为角色养成游戏，激励学生持续进步。

---

## 项目背景

- **发起人**：数学老师
- **目标**：为初中生打造轻量级成绩激励系统，无氪金，关注成长过程而非单纯竞争
- **技术选型**：微信小程序 + 云开发（云数据库 + 云函数）
- **AppID**：`wxa957f595e5a52379`（教育类目）
- **云环境 ID**：`cloud1-3g0pzu4pe8d12b17`
- **开发启动**：2026-04-01

---

## v1.0.0 · 初始版本

**发布日期**：2026-04-01

### 核心功能

#### 学生端
- 用班级密钥登录，首次进入角色创建页
- 随机获得 24-30 种天赋之一（6大类）
- 查看个人角色页：六维雷达图、属性数值、等级、经验记录
- 查看班级排行榜

#### 教师端
- 用教师密钥登录
- 课堂打分：点击学生 +1 EXP
- 导入 Excel 成绩：按排名自动分配经验值（第1名150EXP，末名50EXP，线性插值）
- 班级管理：查看学生列表、导入名单

#### 天赋系统（6大类）

| 类别 | 特点 | 代表天赋 |
|------|------|---------|
| 探索者 | 智识+灵感高成长 | 星图探索者、海图探索者 |
| 铸造者 | 专注+毅力高成长 | 寒冰铸造者、光明铸造者 |
| 编织者 | 表达+灵感高成长 | 语言编织者、梦境编织者 |
| 守护者 | 心志高成长 | 圣盾守护者、黎明守护者 |
| 引导者 | 智识+表达均衡 | 光之引导者、时之引导者 |
| 突破者 | 某一属性极高 | 黄金突破者、钢铁突破者 |

#### 六维属性

| 属性 | 颜色 | 成长来源 |
|------|------|---------|
| 智识 | 紫 | 考试成绩 |
| 专注 | 橙 | 课堂表现 |
| 毅力 | 绿 | 作业完成率 |
| 灵感 | 粉 | 创意发挥 |
| 表达 | 蓝 | 答题条理 |
| 心志 | 红 | 综合稳定性 |

### 主要修复（开发过程）

1. `project.config.json`：`compileType` 从 `game` 改为 `miniprogram`（根因：用户首次注册时误选"游戏"类目）
2. `app.json`：删除不存在的 `pages/index/index`
3. 创建缺失的 `sitemap.json`
4. 生成 6 个 tabBar 图标 PNG
5. `app.js`：云环境 ID 修正（用户误将 AppID 当云环境 ID）
6. `login.js`：修复教师登录逻辑
7. 全局替换旧头像引用为 AvatarManager
8. 修复 4 个空云函数文件夹，修复 4 个 `package.json` 缺少 `wx-server-sdk` 依赖声明
9. `app.wxss`：去掉中文注释解决编码错误

> **教训**：云函数 `package.json` 必须声明 `wx-server-sdk` 依赖，否则上传后调用失败

---

## v1.0.1 · 升级系统重设计 & 自定义 tabBar

**发布日期**：2026-04-01

### 更新内容

1. **升级系统重设计**
   - 等级上限：20级 → **100级**
   - 前3级只需 1 EXP（每节课都能升级，增强初期成就感）
   - 预期：一学期顶尖学生约达 Lv47-50，普通学生约达 Lv34

2. **自定义 tabBar**
   - 学生端：只显示「我的角色」+「排行榜」
   - 教师标签对学生不可见

---

## v1.1.0 · 登录系统重构 & 教师班级管理

**发布日期**：2026-04-01

### 重大变更：学生登录方式

- **原方案**（存在 Bug）：用班级密钥 → 用 openid 查找学生 → 学生第一次登录时 openid 为空，永远找不到
- **新方案**：班级密钥 + **个人密钥（6位字母）** → 用 `classId + studentKey` 精确查找 → 首次登录自动绑定 openid

### 教师班级管理功能

1. 页面顶部展示班级密钥 + 教师密钥，支持一键复制
2. **补发密钥**：为旧学生（无 studentKey）自动生成个人密钥
   - 修复：前端直接 `db.update()` 受安全规则限制 → 新增 `fixStudentKeys` 云函数（管理员权限）
3. **导出密钥**：一键复制全部学生密钥到剪贴板
4. 学生卡片操作栏：移出班级 / 永久删除（二次确认 + 清除日志）

### Bug 修复

- 修复 tabBar：学生端不再显示教师标签占位
- 修复导入弹窗：点击 textarea 不再意外关闭

---

## v1.2.0 · 自定义经验规则 & 导入增强

**发布日期**：2026-04-01

### 导入成绩功能升级

1. **自定义经验值范围**：教师可设置最高分/最低分对应的 EXP 上下限
2. **4种递减规则**：
   - 线性（均匀递减）
   - 指数（头部学生优势更大）
   - 对数（尾部学生差距缩小）
   - 正态（中间多、两端少）
3. **实时预览**：配置规则后可预览各排名对应的 EXP

### 导入匹配增强

- 支持**学号 + 姓名**双重匹配（优先学号，其次姓名）
- 详细错误提示：显示未匹配学生列表、解析错误行号及原因

### 其他修复

- 新增 `deleteStudent` 云函数（解决前端直接删除数据库的权限问题）
- 修复 Excel 解析空行问题
- 修复 `teacher-upload.js` 第121行变量名错误（`s` 改为 `matched`）
- 移除无效的"移出班级"按钮（暂无可转移班级）

---

## v1.3.0 · 每日任务系统 & 数据库权限全面重构

**发布日期**：2026-04-01

### 核心问题发现 & 修复

**问题**：学生提交任务后教师端看不到，因为前端直接操作数据库受安全规则限制（只能读写自己创建的数据）。

**解决方案**：将所有前端直接数据库操作全部改为云函数调用（云函数有管理员权限）。

### 新增云函数

| 云函数 | 功能 |
|--------|------|
| `submitTask` | 学生提交任务完成 |
| `getPendingTasks` | 教师获取待确认任务列表 |
| `getStudentData` | 获取学生最新数据 |
| `login` | 统一处理教师/学生登录 |
| `getClassData` | 统一获取班级相关数据（学生列表、统计、日志、排行榜） |
| `updateStudent` | 更新学生信息（移出班级、赠予重置机会等） |
| `undoExp` | 撤回经验值操作（标记日志 undone + 扣除 EXP） |

### 前端改造范围

- `login.js`：改用 `login` 云函数
- `character.js`：改用 `submitTask` / `getStudentData` 云函数
- `teacher.js`：改用 `getClassData` 云函数
- `teacher-class.js`：改用 `getClassData` / `getPendingTasks` / `updateStudent` 云函数
- `teacher-score.js`：改用 `getClassData` 云函数
- `teacher-upload.js`：改用 `getClassData` 云函数
- `ranking.js`：改用 `getClassData` 云函数

### 每日任务功能

- 学生每天自动分配1个任务（可刷新3次）
- 任务完成需教师确认，奖励10 EXP
- 天赋匹配的任务额外 +5 EXP

### 近期操作 & 撤回

- 教师端显示近期经验值变动记录（支持课堂打分、考试导入、任务确认三类）
- 点击操作记录弹出详情，可一键撤回
- 统一 `expLogs` 数据结构：`amount` / `description` / `createdAt` / `undone`

### Bug 修复

- 学生名显示"未知"问题：`getClassData` 云函数改为查全班学生后本地 map 匹配
- 任务日志不显示：前端 type 过滤器补充 `'task'` 类型

### UI 优化（教师端）

- 任务确认弹窗改为卡片式布局（头部/内容/底部）
- 近期操作改为列表+详情弹窗两层设计
- 修复弹窗内容过于靠左（增加 `padding: 40rpx`）
- 修复学生卡片删除按钮溢出（移除 flex-wrap，调整 padding）
- 修复任务确认按钮宽度不均（统一 `flex: 1`）

---

## v1.3.1 · 班级管理 UI 优化

**发布日期**：2026-04-01

### 问题

学生卡片底部「EXP + 操作按钮」在同一行，空间不足导致「删除」按钮文字被挤压截断。

### 修复

- EXP 显示单独一行：带浅紫渐变背景，标签左、数值右
- 操作按钮单独一行：重置/删除各占50%宽度，等高等宽

---

## v1.4.0 · 任务池随机分配 & 登录流程优化

**发布日期**：2026-04-02

### 核心变更：任务池随机分配

- 学生登录时从任务池随机抽取任务，不再使用固定顺序
- 每次登录/刷新可能获得不同任务，增加新鲜感

### 登录流程优化

- 移除前端独立的 `getOpenId` 调用
- 云函数 `login` 内部直接通过 `cloud.getWXContext().OPENID` 获取
- 登录从 2 次云函数调用减少为 1 次，解决网络延迟时的 timeout 报错

### 其他优化

- `updateStudent` 云函数：支持更灵活的学生信息更新操作
- 角色创建流程：去除重复随机逻辑，流程更清晰
- 角色页任务卡片：样式更紧凑，布局更合理

---

## v1.3.2 · 登录超时修复

**发布日期**：2026-04-02

### 问题

学生登录时控制台频繁出现 `Error: timeout`，但不影响使用。

### 原因

`_studentLogin` 方法连续调用了两次云函数：
1. 第一次：`login` 云函数获取 openid
2. 第二次：`login` 云函数进行实际登录

双重请求在网络稍有延迟时触发超时。

### 修复

- 前端移除单独的 `getOpenId` 请求
- 云函数 `studentLogin` 内部直接通过 `cloud.getWXContext().OPENID` 获取 openid
- 登录从 **2次** 云函数调用减少为 **1次**

---

## 数据库集合说明

| 集合 | 说明 |
|------|------|
| `classes` | 班级信息（name / studentKey / teacherKey） |
| `students` | 学生角色数据（天赋、属性、等级、经验、密钥等） |
| `expLogs` | 经验值变动日志（type / amount / description / createdAt / undone） |
| `tasks` | 每日任务记录（学生提交 / 教师确认状态） |

---

## 已知限制 & 待办

- [x] 称号系统 v1.6.1（综合六维属性分布）✅ 已实现 (v1.6.1)
- [ ] 每日任务的动态徽章系统（连续行为追踪）尚未完成
- [ ] 课堂打分自动化加成逻辑（根据历史行为判断）尚未实现
- [ ] 创建角色流程改造：先选天赋大类，再随机子类型

---

## v1.6.0 · 特殊任务刷新逻辑 & 任务卡片优化

**发布日期**：2026-04-02

### 新增功能

#### 1. 学生端特殊任务刷新确认机制
- 学生刷新特殊任务时弹出确认弹窗，告知稀有性和风险
- 确认后刷新为普通任务（不再分配特殊任务）
- 已完成/放弃的特殊任务记录到 `completedSpecialTaskIds` 字段
- 重置后不再分配已完成过的特殊任务

#### 2. 任务卡片 UI 重设计
- 重新设计自定义任务卡片样式，更现代美观
- 添加分类选择器，支持 7 种职业分类（探索系/锻造系/编织系/守护系/引导系/突破系/通用）
- 分类标签显示中文名称，不再显示英文

### Bug 修复

#### 编辑任务弹窗报错
- **问题**：点击编辑按钮时出现错误
- **原因**：未定义 `categoryOptions`
- **修复**：在 data() 中添加分类选项数组

### 部署清单

使用 v1.6.0 前需要完成以下配置：

1. **上传云函数**：
   - [ ] `assignDailyTask`（更新已存在的云函数）

---

## v1.5.0 · 任务管理独立页面 & 特殊任务系统

**发布日期**：2026-04-02

### 新增功能

#### 1. 任务管理独立页面
新建 `pages/teacher-task/` 模块，整合任务相关管理功能：
- **Tab 1 - 特殊任务**：发布/编辑/删除特殊任务（固定显示在学生任务列表首位）
- **Tab 2 - 任务池**：管理自定义任务 + 查看内置任务
- **Tab 3 - 待确认**：处理学生提交的任务，支持一键确认

#### 2. 教师端任务重置功能
- **重置单个学生**：在待确认列表每个任务卡片上增加「重置」按钮
- **重置全班任务**：一键重置全班学生今天的任务，让学生可重新获取

#### 3. 每日自动刷新任务（待配置定时触发器）
新增云函数 `dailyTaskRefresh`，每天早上6点自动执行：
- 学生今天没有任务 → 分配新任务
- 学生有任务且未完成（pending）且未刷新过 → 保留原任务
- 学生有任务且已提交（submitted）或已刷新过 → 重新分配

### 新增云函数

| 云函数 | 功能 |
|--------|------|
| `manageSpecialTask` | 管理特殊任务（发布/修改/删除/获取） |
| `manageTaskPool` | 管理自定义任务池 |
| `confirmTask` | 教师确认/拒绝任务，发放奖励 |
| `resetTaskProgress` | 重置学生任务进度（单个/全班） |
| `dailyTaskRefresh` | 每日自动刷新任务（定时触发） |

### Bug 修复记录

#### Tab 切换白屏问题
- **问题**：点击 Tab 后页面卡住，所有操作无响应
- **原因**：
  1. `data-index="0"` 传的是字符串，但 `currentTab === 0` 比较时类型不匹配
  2. loading 遮罩层 z-index 过高覆盖了 Tab 导航
- **修复**：
  1. `switchTab` 中使用 `parseInt()` 确保索引是数字
  2. 移除 loading 遮罩层，改用下拉刷新

#### 任务确认显示失败问题
- **问题**：任务实际确认成功，但页面显示"失败"
- **原因**：前端判断 `res.result.success` 不够健壮
- **修复**：兼容多种成功返回格式

### 部署清单

使用 v1.5.0 前需要完成以下配置：

1. **上传云函数**（4个新增）：
   - [ ] `manageSpecialTask`
   - [ ] `manageTaskPool`
   - [ ] `confirmTask`
   - [ ] `resetTaskProgress`
   - [ ] `dailyTaskRefresh`

2. **创建数据库集合**（如不存在）：
   - [ ] `specialTasks` - 特殊任务
   - [ ] `taskPool` - 自定义任务池

3. **配置定时触发器**（每日6点）：
   - 在云开发控制台 → 云函数 → `dailyTaskRefresh` → 触发器
   - 添加触发器：`cron: 0 6 * * *`（每天早上6点）

---

## 技术规范

- 云函数 `package.json` **必须**声明 `wx-server-sdk` 依赖
- 所有数据库写操作**必须**通过云函数（管理员权限），不能在前端直接操作
- expLogs 字段规范：`amount`（数字）/ `description`（字符串）/ `createdAt`（时间戳）/ `undone`（布尔）/ `type`（'score'/'class'/'task'）
- WXML `data-*` 属性永远是字符串，比较时需要 `parseInt()` 转换或使用 `==` 非严格相等

---

## v1.7.0 · 称号系统

**发布日期**：2026-04-02

### 新增功能：动态称号系统

根据学生**六维属性中最高的那一项**，自动授予一个响亮的称号。

#### 称号定义

| 最高属性 | 称号系列 | Lv.1-10 | Lv.11-25 | Lv.26+ |
|---------|---------|---------|---------|--------|
| 智识 | 学神 | ·启明 | ·洞察 | ·天算 |
| 专注 | 战神 | ·静心 | ·入定 | ·无我 |
| 毅力 | 勇者 | ·初征 | ·砥柱 | ·不屈 |
| 灵感 | 天才 | ·火花 | ·泉涌 | ·鬼斧 |
| 表达 | 名师 | ·初晓 | ·传道 | ·圣贤 |
| 心志 | 圣者 | ·觉醒 | ·坚壁 | ·不朽 |

#### 规则说明

- **称号更新时机**：每次升级时自动计算并更新
- **属性相同决胜**：智识 > 毅力 > 心志 > 专注 > 灵感 > 表达
- **称号颜色**：与对应属性颜色一致
- **称号存储**：`students` 集合新增 `title` 和 `titleColor` 字段

#### 前端展示

- 角色页顶部英雄卡片：称号标签显示（带颜色边框）
- 排行榜：每个学生名字旁显示称号

### 代码改动

#### 游戏数据层
- `gameData.js`：新增 `calcTitle()` 函数、`TITLE_DEFINITIONS` 配置、`ATTR_PRIORITY` 属性优先级

#### 云函数
- `addExp`：课堂加分时检查升级，更新称号
- `confirmTask`：任务完成确认时检查升级，更新称号

#### 前端页面
- `character.js`：导入 `calcTitle`，计算并展示称号
- `character.wxml`：英雄卡片添加称号标签
- `character.wxss`：添加 `.title-tag` 样式
- `ranking.js`：更新称号计算逻辑，使用新的 `calcTitle`
- `ranking.wxml`：排行榜添加称号文字显示
- `ranking.wxss`：添加 `.rank-title-text` 样式

### 部署清单

使用 v1.7.0 前需要完成以下配置：

1. **上传云函数**（2个更新）：
   - [ ] `addExp`（更新已存在的云函数）
   - [ ] `confirmTask`（更新已存在的云函数）

2. **上传前端代码**：
   - [ ] `miniprogram/utils/gameData.js`
   - [ ] `miniprogram/pages/character/` 目录
   - [ ] `miniprogram/pages/ranking/` 目录

---

## v1.6.1 · 称号系统 v2.0（综合六维分布）

**发布日期**：2026-04-03

### 重磅升级：综合六维属性分布的动态称号

v1.7.0 的称号系统只考虑最高属性，v2.0 升级为**综合分析六维属性分布**，根据不同形态授予不同称号，大幅提升多样性！

#### 称号格式

```
[属性类型称号][档位名][档内等级]
示例：学神·启明·初阶、鬼才·洞察·精通、六边形·天算·大成
```

#### 四种属性分布类型

| 类型 | 判断条件 | 基础称号 |
|------|----------|---------|
| **单峰型** | 最高 > 次高×1.3 | 学神/战神/勇者/天才/名师/圣者 |
| **双峰型** | 前二高 > 第三×1.3 | 鬼才/博文/铁壁/静心/铁魂/妙语 |
| **均衡型** | 标准差 < 平均×0.2 | 六边形 |
| **专精型** | 最低 < 平均×0.7 | 行动派/灵感派/速攻派/实务派/沉思者/热血派 |

#### 双峰型组合

| 组合 | 称号 | 颜色 |
|------|------|------|
| 智识+灵感 | 鬼才 | #a855f7 |
| 智识+表达 | 博文 | #3b82f6 |
| 专注+毅力 | 铁壁 | #f59e0b |
| 专注+心志 | 静心 | #ef4444 |
| 毅力+心志 | 铁魂 | #10b981 |
| 灵感+表达 | 妙语 | #ec4899 |

#### 档位（与升级曲线互补）

| 档位 | 等级范围 | 后缀 |
|------|---------|------|
| 启明档 | Lv.1-10 | ·启明 |
| 洞察档 | Lv.11-25 | ·洞察 |
| 天算档 | Lv.26+ | ·天算 |

#### 档内等级（7档）

| 档内等级 | 覆盖等级 | 名称 |
|---------|---------|------|
| 1 | Lv.1-20 | 初阶 |
| 2 | Lv.21-30 | 入门 |
| 3 | Lv.31-40 | 登堂 |
| 4 | Lv.41-55 | 小成 |
| 5 | Lv.56-70 | 精通 |
| 6 | Lv.71-85 | 大成 |
| 7 | Lv.86-100 | 化境 |

**设计原则**：升级越难，称号越容易升，形成互补曲线。

### 代码改动

#### 游戏数据层
- `gameData.js`：完全重写 `calcTitle()` 函数，新增多种称号配置

#### 云函数（2个更新）
- `addExp/index.js`：更新称号计算逻辑为 v2.0
- `confirmTask/index.js`：更新称号计算逻辑为 v2.0

### 部署清单

使用 v1.6.1 前需要完成以下配置：

1. **上传云函数**（必须重新上传）：
   - [ ] `addExp`
   - [ ] `confirmTask`

2. **上传前端代码**：
   - [ ] `miniprogram/utils/gameData.js`

> ⚠️ 注意：v2.0 修改了称号格式（如 `学神·启明` → `学神·启明·初阶`），旧学生会看到称号变化，这是预期行为。
