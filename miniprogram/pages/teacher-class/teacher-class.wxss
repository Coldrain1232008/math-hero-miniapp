/* pages/teacher-class/teacher-class.wxss */
.page-bg { background: #f0f0f8; min-height: 100vh; padding-bottom: 80rpx; }

/* 班级信息卡片 */
.class-info-card {
  background: #fff;
  border-radius: 20rpx;
  padding: 32rpx 28rpx;
  margin: 20rpx 24rpx;
  box-shadow: 0 4rpx 16rpx rgba(0,0,0,0.05);
}
.class-name {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
  display: block;
  margin-bottom: 24rpx;
  padding-bottom: 20rpx;
  border-bottom: 1rpx solid #eee;
}
.key-row { margin-bottom: 24rpx; }
.key-item { margin-bottom: 16rpx; }
.key-label {
  font-size: 22rpx;
  color: #999;
  display: block;
  margin-bottom: 8rpx;
}
.key-value-box {
  display: flex;
  align-items: center;
  gap: 16rpx;
  background: #f8f8fc;
  border-radius: 10rpx;
  padding: 12rpx 16rpx;
}
.key-value {
  font-size: 30rpx;
  font-weight: bold;
  color: #6c63ff;
  font-family: 'Menlo', 'Consolas', monospace;
  letter-spacing: 4rpx;
  flex: 1;
}
.key-value.teacher-key { color: #f59e0b; }
.btn-edit-sm {
  font-size: 22rpx;
  padding: 6rpx 20rpx;
  background: #fff7ed;
  color: #f59e0b;
  border-radius: 20rpx;
  border: 1rpx solid #fde68a;
  margin: 0;
  line-height: 1.6;
  white-space: nowrap;
}
.btn-copy-sm {
  font-size: 22rpx;
  padding: 6rpx 20rpx;
  background: #e8e5ff;
  color: #6c63ff;
  border-radius: 20rpx;
  border: none;
  margin: 0;
  line-height: 1.6;
  white-space: nowrap;
}

.action-row {
  display: flex;
  gap: 16rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid #eee;
}
.btn-action {
  flex: 1;
  font-size: 26rpx;
  padding: 16rpx 0;
  background: #f0f4ff;
  color: #6c63ff;
  border-radius: 12rpx;
  border: none;
  margin: 0;
  text-align: center;
}
.btn-action:active { background: #e0e5ff; }

.btn-action.task-btn {
  position: relative;
}

.task-badge {
  position: absolute;
  top: -8rpx;
  right: -8rpx;
  background: #ef4444;
  color: #fff;
  font-size: 20rpx;
  font-weight: bold;
  padding: 4rpx 12rpx;
  border-radius: 20rpx;
  min-width: 32rpx;
  text-align: center;
}

/* 列表头 */
.header-bar { display: flex; justify-content: space-between; align-items: center; padding: 20rpx 24rpx 0; }
.total-label { font-size: 26rpx; color: #888; }
.btn-add { background: #6c63ff; color: #fff; border-radius: 40rpx; font-size: 26rpx; padding: 12rpx 32rpx; border: none; margin: 0; }

/* 学生列表 */
.student-list { padding: 0 24rpx; }
.s-card { background: #fff; border-radius: 20rpx; padding: 28rpx; margin-bottom: 20rpx; box-shadow: 0 4rpx 16rpx rgba(0,0,0,0.05); }
.s-top { display: flex; align-items: center; gap: 20rpx; margin-bottom: 20rpx; }
.s-avatar-circle { width: 80rpx; height: 80rpx; border-radius: 50%; display: flex; align-items: center; justify-content: center; }
.s-avatar-icon { font-size: 40rpx; }
.s-main { flex: 1; }
.s-id-name-row { display: flex; align-items: center; gap: 16rpx; margin-bottom: 4rpx; }
.s-student-id { font-size: 24rpx; color: #6c63ff; font-weight: 600; background: #f0f4ff; padding: 2rpx 10rpx; border-radius: 6rpx; }
.s-real-name { font-size: 30rpx; font-weight: bold; color: #333; }
.s-hero-name { font-size: 26rpx; color: #666; display: block; margin-bottom: 6rpx; }
.s-hero-name.pending { color: #999; font-style: italic; }
.s-talent-tag { font-size: 22rpx; padding: 3rpx 12rpx; background: rgba(0,0,0,0.04); border-radius: 8rpx; display: inline-block; }
.s-level-badge { background: linear-gradient(135deg, #f6d365, #fda085); color: #fff; font-size: 22rpx; font-weight: bold; padding: 6rpx 16rpx; border-radius: 20rpx; }

.s-attrs { display: flex; justify-content: space-between; background: #f8f8fc; border-radius: 12rpx; padding: 16rpx 20rpx; margin-bottom: 16rpx; }
.mini-attr { display: flex; flex-direction: column; align-items: center; }
.mini-attr text { font-size: 20rpx; font-weight: 600; }
.mini-val { font-size: 24rpx; color: #333; margin-top: 4rpx; }

.s-key-section { display: flex; align-items: center; gap: 12rpx; margin-bottom: 16rpx; padding: 12rpx 16rpx; background: #f0f4ff; border-radius: 10rpx; }
.s-key-label { font-size: 22rpx; color: #888; white-space: nowrap; }
.s-key-box { display: flex; align-items: center; gap: 12rpx; flex: 1; }
.s-key-text { font-size: 28rpx; font-weight: bold; color: #6c63ff; font-family: 'Menlo', 'Consolas', monospace; letter-spacing: 4rpx; }
.s-key-text.no-key { color: #ccc; font-size: 24rpx; }
.btn-copy { font-size: 22rpx; padding: 6rpx 20rpx; background: #6c63ff; color: #fff; border-radius: 20rpx; border: none; margin: 0; line-height: 1.6; }

.s-footer { display: flex; justify-content: space-between; align-items: center; }
.s-exp { font-size: 24rpx; color: #999; }
.s-actions { display: flex; gap: 12rpx; }

/* 经验值显示行 */
.s-exp-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16rpx 20rpx;
  background: linear-gradient(135deg, #f0f4ff, #f8f8fc);
  border-radius: 12rpx;
  margin-bottom: 16rpx;
}
.s-exp-label {
  font-size: 22rpx;
  color: #888;
}
.s-exp-value {
  font-size: 28rpx;
  font-weight: bold;
  color: #6c63ff;
  font-family: 'Menlo', 'Consolas', monospace;
}

/* 操作按钮行 */
.s-actions-row {
  display: flex;
  gap: 16rpx;
}
.s-actions-row .btn-sm {
  flex: 1;
  font-size: 24rpx;
  padding: 14rpx 0;
  border-radius: 30rpx;
  margin: 0;
  border: none;
  white-space: nowrap;
  line-height: 1.4;
  text-align: center;
}
.btn-sm { font-size: 22rpx; padding: 10rpx 16rpx; border-radius: 30rpx; margin: 0; border: none; white-space: nowrap; line-height: 1.4; }
.btn-reroll { background: #fef3c7; color: #b45309; }
.btn-delete { background: #ef4444; color: #fff; }

/* 弹窗 */
.modal-mask {
  position: fixed; top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.5); z-index: 100;
  display: flex; align-items: center; justify-content: center;
}
.modal-box { background: #fff; border-radius: 24rpx; padding: 48rpx 40rpx; width: 80%; }
.modal-title { font-size: 34rpx; font-weight: bold; color: #333; display: block; margin-bottom: 12rpx; }
.modal-desc { font-size: 24rpx; color: #999; display: block; margin-bottom: 28rpx; }
.name-textarea { width: 100%; height: 200rpx; background: #f8f8fc; border-radius: 12rpx; padding: 20rpx; font-size: 28rpx; color: #333; box-sizing: border-box; }
.modal-btns { display: flex; gap: 20rpx; margin-top: 32rpx; }
.modal-btns button { flex: 1; }
.btn-secondary { background: transparent; color: #6c63ff; border: 2rpx solid #6c63ff; border-radius: 40rpx; font-size: 28rpx; padding: 14rpx 36rpx; }
.btn-primary { background: #6c63ff; color: #fff; border-radius: 40rpx; font-size: 28rpx; padding: 14rpx 36rpx; border: none; }

/* 任务确认弹窗 */
.task-modal {
  max-height: 80vh;
  display: flex;
  flex-direction: column;
}

.modal-header {
  margin-bottom: 24rpx;
}

.modal-subtitle {
  font-size: 26rpx;
  color: #666;
  display: block;
  margin-top: 8rpx;
}

.task-list {
  flex: 1;
  max-height: 500rpx;
}

.task-empty {
  text-align: center;
  color: #999;
  padding: 60rpx 0;
}

/* 任务卡片 */
.task-card {
  background: #f8f8fc;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
}

.task-card:last-child {
  margin-bottom: 0;
}

/* ========== 修改密钥弹窗（浅色主题，配合本页） ========== */
.key-edit-modal { width: 84%; }

.modal-sub {
  font-size: 24rpx;
  color: #999;
  display: block;
  margin-bottom: 28rpx;
}

.current-key-row {
  display: flex;
  align-items: center;
  gap: 8rpx;
  background: #f8f8fc;
  border-radius: 12rpx;
  padding: 18rpx 20rpx;
  margin-bottom: 28rpx;
}
.current-key-label { font-size: 24rpx; color: #999; }
.current-key-value {
  font-size: 30rpx;
  font-weight: bold;
  color: #333;
  font-family: 'Menlo', 'Consolas', monospace;
  letter-spacing: 4rpx;
}

.modal-input-group { margin-bottom: 20rpx; }
.modal-label { font-size: 24rpx; color: #666; display: block; margin-bottom: 12rpx; }
.modal-input {
  width: 100%;
  box-sizing: border-box;
  background: #f8f8fc;
  border: 2rpx solid #e8e5ff;
  border-radius: 12rpx;
  padding: 20rpx;
  font-size: 32rpx;
  color: #333;
  font-family: 'Menlo', 'Consolas', monospace;
  letter-spacing: 4rpx;
}

/* placeholder 自渲染（绕过 native input 渲染 bug）
   ⚠️ <text> 必须 display:block 且配 left+right 锁定宽度，否则 ellipsis 失效 */
.ph-wrap {
  position: relative;
  width: 100%;
}
.ph {
  display: block;
  position: absolute;
  left: 20rpx;
  right: 20rpx;
  top: 50%;
  transform: translateY(-50%);
  height: 45rpx;
  line-height: 45rpx;
  font-size: 32rpx;
  color: #999;
  pointer-events: none;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.ph-textarea-wrap {
  width: 100%;
  height: 200rpx;
}
.ph-textarea {
  display: block;
  position: absolute;
  left: 20rpx;
  right: 20rpx;
  top: 20rpx;
  font-size: 28rpx;
  line-height: 40rpx;
  color: #999;
  pointer-events: none;
  white-space: pre-wrap;
  overflow: hidden;
}

.key-check {
  font-size: 24rpx;
  line-height: 1.5;
  padding: 12rpx 16rpx;
  border-radius: 10rpx;
  margin-bottom: 20rpx;
}
.key-check.checking { color: #6b7280; background: #f3f4f6; }
.key-check.ok { color: #059669; background: #ecfdf5; }
.key-check.bad { color: #dc2626; background: #fef2f2; }
.key-check.same { color: #b45309; background: #fffbeb; }

.modal-tip {
  font-size: 22rpx;
  color: #999;
  line-height: 1.6;
  margin-bottom: 32rpx;
}
.modal-tip.warn { color: #d97706; }

.modal-btn-row { display: flex; gap: 20rpx; }
.modal-btn {
  flex: 1;
  font-size: 28rpx;
  height: 80rpx;
  line-height: 80rpx;
  text-align: center;
  border-radius: 40rpx;
  border: none;
  margin: 0;
  padding: 0;
}
.modal-btn.cancel { background: #f3f4f6; color: #666; }
.modal-btn.confirm { background: #6c63ff; color: #fff; font-weight: 600; }
.modal-btn.confirm[disabled] { background: #c7c5f5; color: #fff; }

/* 头部：学生信息 */
.task-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
  padding-bottom: 16rpx;
  border-bottom: 1rpx solid #e8e8f0;
}

.task-student-info {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.task-student-name {
  font-size: 30rpx;
  font-weight: bold;
  color: #333;
}

.task-student-id {
  font-size: 22rpx;
  color: #6c63ff;
  background: #e8e5ff;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
}

.task-submit-time {
  font-size: 22rpx;
  color: #999;
}

/* 中部：任务内容 */
.task-body {
  margin-bottom: 20rpx;
}

.task-title {
  font-size: 28rpx;
  color: #333;
  font-weight: 600;
  display: block;
  margin-bottom: 8rpx;
}

.task-desc {
  font-size: 26rpx;
  color: #666;
  display: block;
  line-height: 1.5;
}

/* 底部：经验值和操作 */
.task-footer {
  padding-top: 16rpx;
  border-top: 1rpx solid #e8e8f0;
}

.task-reward {
  display: inline-flex;
  align-items: center;
  gap: 12rpx;
  background: #f0f9f4;
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  margin-bottom: 16rpx;
}

.reward-label {
  font-size: 22rpx;
  color: #999;
}

.reward-value {
  font-size: 28rpx;
  color: #10b981;
  font-weight: bold;
}

.task-actions {
  display: flex;
  gap: 16rpx;
}

.btn-task-reject, .btn-task-confirm {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  padding: 16rpx 32rpx;
  border-radius: 30rpx;
  border: none;
  margin: 0;
  min-width: 140rpx;
}

.btn-task-reject {
  background: #fee2e2;
  color: #ef4444;
}

.btn-task-confirm {
  background: #6c63ff;
  color: #fff;
}

.btn-icon {
  font-size: 24rpx;
}

.btn-text {
  font-size: 26rpx;
}

/* ===== 危险操作区（删除班级） ===== */
.danger-zone {
  margin-top: 24rpx;
  padding-top: 20rpx;
  border-top: 1rpx dashed #f3c2c2;
}

.danger-head {
  display: flex;
  align-items: baseline;
  gap: 12rpx;
  margin-bottom: 10rpx;
}

.danger-title {
  font-size: 26rpx;
  font-weight: 600;
  color: #ef4444;
}

.danger-sub {
  font-size: 22rpx;
  color: #c99;
}

.danger-desc {
  display: block;
  font-size: 23rpx;
  color: #888;
  line-height: 1.6;
  margin-bottom: 18rpx;
}

.danger-strong {
  color: #ef4444;
  font-weight: 600;
}

.btn-danger {
  width: 100%;
  box-sizing: border-box;
  font-size: 26rpx;
  padding: 16rpx 0;
  background: #fef2f2;
  color: #ef4444;
  border: 2rpx solid #fecaca;
  border-radius: 12rpx;
  margin: 0;
  line-height: 1.4;
  text-align: center;
}

.btn-danger:active {
  background: #fee2e2;
}

/* ===== 删除班级弹窗 ===== */
.delete-modal {
  width: 86%;
}

.delete-block {
  display: flex;
  gap: 14rpx;
  align-items: flex-start;
  background: #fef2f2;
  border: 2rpx solid #fecaca;
  border-radius: 12rpx;
  padding: 24rpx;
  margin-bottom: 8rpx;
}

.delete-block-icon {
  font-size: 32rpx;
  line-height: 1.4;
}

.delete-block-text {
  flex: 1;
  font-size: 26rpx;
  color: #b91c1c;
  line-height: 1.65;
}

.delete-ok {
  font-size: 26rpx;
  color: #059669;
  background: #ecfdf5;
  border-radius: 12rpx;
  padding: 16rpx 20rpx;
  margin-bottom: 20rpx;
}

.delete-scope {
  background: #f8f8fc;
  border-radius: 12rpx;
  padding: 20rpx;
  margin-bottom: 24rpx;
}

.delete-scope-title {
  display: block;
  font-size: 24rpx;
  color: #666;
  margin-bottom: 10rpx;
}

.delete-scope-item {
  display: block;
  font-size: 23rpx;
  color: #999;
  line-height: 1.7;
}

.delete-name-check {
  margin: -8rpx 0 20rpx;
  font-size: 24rpx;
}

.check-ok {
  color: #059669;
}

.check-bad {
  color: #ef4444;
}

.modal-btn.danger {
  background: #ef4444;
  color: #fff;
  font-weight: 600;
}

.modal-btn.danger[disabled] {
  background: #f3b5b5;
  color: #fff;
}
